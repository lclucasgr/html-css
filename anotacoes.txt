seletores universal
* {margin:0; padding:0; color:red;}


//seletor tag

h1
{
	color: blue;
	font-size: 50px;
}

p
{
	color: red;
	font-size: 25px;
}

input
{
	width: 200px;
	background-color: gray;
	height: 40px;
	border: none;
}	

input[type="text"]
{
	width: 200px;
	background-color: gray;
}

id só pode ser escrito uma vez

//padding espaçamento interno
padding-top - Parte superior
padding-right - Parte direito
padding-bottom - Espaçamento na base

padding:
1 - superior 
2 - direita
3 - base 
4 - esquerda

text-decoration: underline, overline, line-through; //sublinhado acima da linha 
text-align: justify;
text-indent: 20px; //cria um recuo na primeira linha
line-height: 30px; altura das linhas
direction: rtl; direita para esquerda
letter-spacing: 5px; espaço entre as letras
word-spacing: espaçamento entre as palavras
text-shadow: posição horizontal, posição vertical, desfoque, cor da sombra; //usado pro box-shadow
box-sizing: border-box; //resolve o problema de espaçamento nas divs, content-box;
overflow: scroll //adiciona barras de rolagem caso o tamanho do texto seja menor que a div, auto //adiciona barras de rolagem onde é necessário
border-radius : 20px //criar um raio de arredondamento e possivel passar mais de um valor; //usado para divs e quadrados
border-top-left-radius: 20px; //borda superior direita
background-color: linear-gradient(green, yellow);
background-color: linear-gradient(to right, green, yellow);
background-color: radial-gradient(red, blue);

position: absolute, left:0; top:0;
position: relative; //adicionar na div pai
position: fixed; //fixa o elemento

border-style: dotted, dashed, solid, double, groove, ridge, inset, outset;
border-width: 5px;
border-color: yellow; ou border: solid 5px yellow;

border-left, right, bottom, top //so adiciona bordas nas extremidades

//usar com hover
transition-property: width;
transition-duration: 2s; //sempre em segundos
transition-timing-function: ease, ease-in, ease-out, ease-in-out, linear;
transition-delay: 2s;
transition: all; //todas as propriedade padrão

background-repeat: repeat-x (repetir imagem na vertical), repeat-y (repetir imagem na horizontal)
background-attachment: fixed; //imagem fixa
background-size: 100% 100%; //vertical e horizontal;

a:link
{
	color: green;
}

a:visited
{
	color: blue;
}

a:active
{
	color: #CCC;
}

a:focus
{
	border: 4px solid #FF0000;
}

a:first-child (primeiro elemento filho)
{

}

a:last-child (ultimo elemento filho)
{

}

display: inline(horizontal, largura e altura não funciona), block(vertical, bloqueia o espaço horizontal da div), none(não apareça no layout);

position: absolute, relative, fixed;

float: flutue um nivel, alinhamento;

clear: (limpar elementos flutuando right, left, both -> impede que entre ao fundo de outras div);

/* Define a posição
display: flex;
flex-direction: column;



























